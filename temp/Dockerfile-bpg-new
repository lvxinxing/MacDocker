FROM tomcat7-jdk7-base:1.0

LABEL app="bpg-new"

# ---------- 可覆盖 JVM 参数 ----------
ENV JAVA_OPTS="-server -Xms512m -Xmx1024m -XX:MaxPermSize=256m -Dfile.encoding=UTF-8"

# ---------- 默认启动（调试友好） ----------
CMD ["catalina.sh", "jpda", "run"]
